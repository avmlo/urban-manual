# Vercel Configuration

This directory contains Vercel-specific configuration files.

## Integration Provisioning

Preview deployments use the same Upstash resources as production to avoid provisioning failures.
Configure integrations in the Vercel dashboard to share resources across environments.
