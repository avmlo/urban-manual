## 2025-05-20 - Tooltips for Icon-only Buttons
**Learning:** Native `title` attributes are insufficient for accessible and responsive tooltips. Users on touch devices or keyboard users cannot easily access `title` content.
**Action:** Replace `title` attributes with Radix UI `Tooltip` components for all icon-only buttons to ensure keyboard accessibility and consistent styling.
