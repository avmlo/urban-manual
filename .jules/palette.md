## 2026-02-04 - Button Loading State
**Learning:** Standardizing loading states in `Button` component (via `isLoading` prop) ensures consistent visual feedback and accessibility (disabled state) across the app, replacing ad-hoc implementations.
**Action:** Use `isLoading` prop on `Button` instead of manually handling spinner and disabled state.
